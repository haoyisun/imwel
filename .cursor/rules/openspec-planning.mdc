---
description: OpenSpec planning artifacts language and workflow for this repo
globs: openspec/**/*
alwaysApply: false
---

# OpenSpec 规划约定

适用于 `openspec/**` 下的变更产物。

## 语言

- `imwel-git-native-cli-mvp` **之后**新建的 change：`proposal.md`、`design.md`、`specs/**/spec.md`、`tasks.md` 使用**简体中文**。
- 已归档的英文 MVP（`openspec/changes/archive/.../imwel-git-native-cli-mvp`）**不要**回翻成中文。
- 源码与 CLI 标识仍须英文（见 `imwel-core` / `AGENTS.md`）——规划语言与源码语言是两套规则。

## 流程

- 用现有 `.cursor/skills/openspec-*` 开变更、续写产物、实现、归档；不要绕过 OpenSpec 直接大改架构。
- 实现前确保 `tasks.md` 可勾选；实现中同步打勾。
- 若实现暴露设计问题，先更新本 change 的 artifacts，再继续写代码。

## 范围

- 每个 change 保持 KISS：只写当前提案需要的能力；后续路线图项（开源发包、CLI 硬化、author 体验、文档站）各自独立 change，勿塞进无关 tasks。
