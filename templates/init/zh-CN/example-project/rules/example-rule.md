---
description: 在本项目中改动代码时使用，保持变更小而聚焦、遵循既有约定。
# globs: ["**/*.ts"]        # 取消注释可将本规则按路径挂载到匹配文件
# alwaysApply: true          # 仅当规则需要始终生效时才取消注释
---

# 示例规则

此规则由 `imwel template init` 为 **{{name}}** 生成。

上方 frontmatter 是可选的元数据 overlay：`description`（何时适用）、`globs`（按路径
挂载）、`alwaysApply`（始终生效）。imwel 会在渲染时将其从正文剥离，并翻译成各工具的
原生格式。

- 保持变更小而聚焦。
- 遵循项目既有约定。
