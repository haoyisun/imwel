---
description: Use when making changes in this project to keep edits small and aligned with existing conventions.
# globs: ["**/*.ts"]        # uncomment to path-attach this rule to matching files
# alwaysApply: true          # uncomment only for a rule that must always be active
---

# Example Rule

This rule was scaffolded by `imwel template init` for **{{name}}**.

The frontmatter above is an optional metadata overlay: `description` (when the rule
applies), `globs` (path-attach), and `alwaysApply` (always-on). imwel strips it from
the canonical body and translates it into each tool's native format at render time.

- Keep changes small and focused.
- Match existing project conventions.
